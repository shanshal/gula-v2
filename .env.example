# Database Configuration
PGUSER=shanshal
PGHOST=localhost
PGDATABASE=gula_v2_app
PGPASSWORD=SecureP@ssword
PGPORT=5432

# Optional: Use DATABASE_URL instead of individual vars
# DATABASE_URL=postgresql://user:password@host:port/database

# SSL Configuration (optional)
# PGSSLMODE=require
# PGSSL=true
# DATABASE_DISABLE_SSL=false
